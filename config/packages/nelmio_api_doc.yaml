nelmio_api_doc:
    documentation:
        info:
            title: Route API de l'application
            description: Nous allons nous servir de toutes ces routes pour finir le projet SmartLeads
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
        

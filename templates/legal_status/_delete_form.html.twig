<form method="post" action="{{ path('legal_status_delete', {'id': legal_status.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_status.id) }}">
    <button class="btn">Delete</button>
</form>

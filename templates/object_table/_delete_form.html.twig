<form method="post" action="{{ path('object_table_delete', {'id': object_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ object_table.id) }}">
    <button class="btn">Delete</button>
</form>

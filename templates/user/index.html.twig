{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
<label>Les utilisateurs sont : </label>
<ul>
    {% for user in User %}
    <a href='{{ path('user_edit',{"id": user.id }) }}'><li>{{ user.name }} {{ user.firstname }}</li></a>
    {% endfor %}

</ul>

<a href="{{ path('user_new') }}">Créer un utilisateur</a>
{% endblock %}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Accueil{% endblock %}</title>
        <!-- Bootstrap core CSS -->
        <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
        <!-- Material Design Bootstrap -->
        <link href="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.7.4/css/mdb.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset("styles.css") }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>

      <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{ path('dashboard') }}">Accueil</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contacts_index') }}">Contacts</a>
                    </li>
                    {% if is_granted("ROLE_DIRECTEUR") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('salesperson_index') }}">Commerciaux</a>
                    </li>
                    {% endif %}
                    {% if is_granted("ROLE_RESPONSABLE") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('salesperson_team') }}">Équipe commerciale</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('company_index') }}">Entreprises</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('operations_index') }}">Opérations</a>
                    </li>
                    {% if is_granted("ROLE_DIRECTEUR") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('settings_index') }}">Paramètres</a>
                    </li>
                    {% endif %}
                    {% if is_granted("IS_AUTHENTICATED_FULLY")%}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                    {% endif %}

                    {% if is_granted("IS_AUTHENTICATED_FULLY")%}
                    <li class="navbar-text" style="color:red;">
                        {{ app.user.lastName ~ " " ~ app.user.firstName ~ " (" ~ app.user.roles[0] ~ ")"}}
                    </li>
                    {% endif %}
                   
                </ul>
            </div>
        </nav>

      
        <div class="container">
        {% block body %}{% endblock %}
        </div>
        
        {% block javascripts %}
            <!-- JQuery -->
            <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
            <!-- Bootstrap tooltips -->
            <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.4/umd/popper.min.js"></script>
            <!-- Bootstrap core JavaScript -->
            <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.min.js"></script>
            <!-- MDB core JavaScript -->
            <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.7.4/js/mdb.min.js"></script>
            <!-- Application Javascript -->
            <script type="text/javascript" src="{{ asset("js/application.js") }}")"></script>
        {% endblock %}
    </body>
</html>

<form method="post" action="{{ path('activity_area_delete', {'id': activity_area.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_area.id) }}">
    <button class="btn">Delete</button>
</form>

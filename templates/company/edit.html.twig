{% extends 'base.html.twig' %}

{% block title %}Modifier une entreprise{% endblock %}

{% block body %}
<a href="{{ path('Company') }}">Retour à la liste des entreprises</a>
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
{{ form_widget(form) }}
{{ form_widget(form.save, { 'label': 'Modifier' }) }}
{{ form_end(form) }}
{% endblock %}

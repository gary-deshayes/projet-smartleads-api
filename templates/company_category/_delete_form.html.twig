<form method="post" action="{{ path('company_category_delete', {'id': company_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_category.id) }}">
    <button class="btn">Delete</button>
</form>

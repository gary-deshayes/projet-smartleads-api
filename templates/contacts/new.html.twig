{% extends 'base.html.twig' %}

{% block title %}Saisie d'un nouveau contact{% endblock %}

{% block body %}
<h1>Contacts</h1>

<div class="window">
    <h5 class="window-description">Saisie d'un nouveau contact</h5>

    <ul class="nav nav-tabs" id="myTab" role="tablist">
        <li class="nav-item">
            <a class="nav-link active" id="identity-tab" data-toggle="tab" href="#identity" role="tab" aria-controls="home"
            aria-selected="true">Identité</a>
        </li>
    </ul>

    <div class="bottom-button">
        <div class="tab-content" id="myTabContent">
            <div class="tab-pane fade show active" id="identity" role="tabpanel" aria-labelledby="identity-tab">
                {{ include('contacts/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            </div>
        </div>

        <a class="btn btn-basic btn-second" href="{{ path('contacts_index') }}">Annuler</a>
    </div>
</div>
{% endblock %}

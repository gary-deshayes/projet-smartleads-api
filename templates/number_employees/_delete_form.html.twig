<form method="post" action="{{ path('number_employees_delete', {'id': number_employee.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ number_employee.id) }}">
    <button class="btn">Delete</button>
</form>

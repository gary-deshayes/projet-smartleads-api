{% extends 'base.html.twig' %}

{% block title %}Operations{% endblock %}

{% block body %}
    <h1>Operations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ operation.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ operation.url }}</td>
            </tr>
            <tr>
                <th>TypeOperation</th>
                <td>{{ operation.typeOperation }}</td>
            </tr>
            <tr>
                <th>VisualHeadband</th>
                <td>{{ operation.visualHeadband }}</td>
            </tr>
            <tr>
                <th>VisuelLateral</th>
                <td>{{ operation.visuelLateral }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operations_index') }}">back to list</a>

    <a href="{{ path('operations_edit', {'name': operation.name}) }}">edit</a>

    {{ include('operations/_delete_form.html.twig') }}
{% endblock %}

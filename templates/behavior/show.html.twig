{% extends 'base.html.twig' %}

{% block title %}Behavior{% endblock %}

{% block body %}
    <h1>Behavior</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ behavior.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ behavior.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('behavior_index') }}">back to list</a>

    <a href="{{ path('behavior_edit', {'id': behavior.id}) }}">edit</a>

    {{ include('behavior/_delete_form.html.twig') }}
{% endblock %}
